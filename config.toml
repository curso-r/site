baseurl = "https://curso-r.github.io/site/"
title = "Curso-R"
theme = "hugo-universal-theme"
languageCode = "pt-br"
# Enable comments by entering your Disqus shortname
disqusShortname = "curso-r"
# Enable Google Analytics by entering your tracking code
googleAnalytics = ""
canonifyURLs = true

# Define the number of posts per page
paginate = 10

[params]
    viewMorePostLink = "/blog/"
    author = "Curso-R"

    # Google Maps API key (if not set will default to not passing a key.)
    googleMapsApiKey = ""

    # Style options: default (light-blue), blue, green, marsala, pink, red, turquoise, violet
    style = "red"

    # Since this template is static, the contact form uses www.formspree.io as a
    # proxy. The form makes a POST request to their servers to send the actual
    # email. Visitors can send up to a 1000 emails each month for free.
    #
    # What you need to do for the setup?
    #
    # - set your email address under 'email' below
    # - upload the generated site to your server
    # - send a dummy email yourself to confirm your account
    # - click the confirm link in the email from www.formspree.io
    # - you're done. Happy mailing!
    #
    # Enable the contact form by entering your Formspree.io email
    email = "your@email.com"

    about_us = "<p>A Curso-R é uma empresa que nasceu com o objetivo de proliferar o conhecimento sobre R, Estatística e Data Science.</p>"
    copyright = "Copyright (c) 2016 - 2017, Curso-R; all rights reserved."

    # Format dates with Go's time formatting
    date_format = "02/01/2006"

    logo = "img/logo-curso-r.png"
    address = """<p><strong>Universal Ltd.</strong>
        <br>13/25 New Avenue
        <br>Newtown upon River
        <br>45Y 73J
        <br>England
        <br>
        <strong>Great Britain</strong>
      </p>
      """
    latitude = "-12.043333"
    longitude = "-77.028333"

[Permalinks]
    blog = "/blog/:year/:month/:day/:filename/"

[[params.menu]]
    name = "Home"
    url  = "/"
    weight = 1

[[params.menu]]
    name = "Cursos"
    url  = "/cursos/"
    weight = 2

[[params.menu]]
    name = "Blog"
    url  = "/blog/"
    weight = 3
    
[[params.menu]]
    name = "Contato"
    url  = "/contact/"
    weight = 4

# Enable and disable widgets for the right sidebar
[params.widgets]
    categories = true
    tags = true
    search = true

[params.carousel]
    enable = true
    # All carousel items are defined in their own files. You can find example items
    # at 'exampleSite/data/carousel'.
    # For more informtion take a look at the README.
    
[params.cursos]
    enable = true
    title = "Nossos cursos"
    subtitle = "Temos diversas opções de cursos que vão desde a introdução à programção em R até tópicos mais avançados ou específicos de uma área do conhecimento como Machine Learning e Web Scraping."

[params.features]
    enable = false
    # All features are defined in their own files. You can find example items
    # at 'exampleSite/data/features'.
    # For more informtion take a look at the README.

[params.testimonials]
    enable = false
    # All testimonials are defined in their own files. You can find example items
    # at 'exampleSite/data/testimonials'.
    # For more informtion take a look at the README.
    title = "Testimonials"
    subtitle = "We have worked with many clients and we always like to hear they come out from the cooperation happy and satisfied. Have a look what our clients said about us."

[params.see_more]
    enable = false
    icon = "fa fa-file-code-o"
    title = "Do you want to see more?"
    subtitle = "We have prepared for you more than 40 different HTML pages, including 5 variations of homepage."
    link_url = "#"
    link_text = "Check other homepages"

[params.clients]
    enable = false
    # All clients are defined in their own files. You can find example items
    # at 'exampleSite/data/clients'.
    # For more informtion take a look at the README.
    title = "Our Clients"
    subtitle = ""

[params.recent_posts]
    enable = true
    title = "Do nosso blog"
    subtitle = "No nosso blog compartilhamos pequenos scripts e análises que podem ser interessantes para o nosso público. Nossos posts sempre mostram análises interessantes que podem ser realizadas usando o R."
